# PyTorch is installed separately so the CUDA build is selected explicitly:
#   uv pip install torch --index-url https://download.pytorch.org/whl/cu124
numpy>=1.26
scipy>=1.11
matplotlib>=3.8
pyyaml>=6.0
pytest>=8.0
